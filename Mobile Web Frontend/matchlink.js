
var popup = document.getElementById('tbd-popup-manage')
var current_query = window.location.search.substring(1)

function g1(){
    var code = 'g1'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
    
}

function g2(){
    var code = 'g2'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g3(){
    var code = 'g3'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g4(){
    var code = 'g4'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g5(){
    var code = 'g5'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g6(){
    var code = 'g6'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g7(){
    var code = 'g7'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function g8(){
    var code = 'g8'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function p1(){
    var code = 'p1'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function p2(){
    var code = 'p2'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function p3(){
    var code = 'p3'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function p4(){
    var code = 'p4'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function q1(){
    var code = 'q1'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function q2(){
    var code = 'q2'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function q3(){
    var code = 'q3'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function q4(){
    var code = 'q4'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function s1(){
    var code = 's1'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function s2(){
    var code = 's2'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function f(){
    var code = 'f'
    var game = getQueryVariable(code)
    var id = getQueryVariable('id')
    var team1 = game.split('-')[0]
    var team2 = game.split('-')[1]
    if(team1 != 0 && team2 != 0){
        var match_query = `/match?id=${id}&code=${code}&team1=${team1}&team2=${team2}`
        window.location = match_query
    }else{
        popup.style.display = 'table'
    }
}

function getQueryVariable(variable) {
    var query = window.location.search.substring(1)
    var vars = query.split('&')
    for (var i = 0; i < vars.length; i++) {
        var pair = vars[i].split('=')
        if (decodeURIComponent(pair[0]) == variable) {
            return decodeURIComponent(pair[1])
        }
    }
    console.log('Query variable %s not found', variable)
}

function hidePopup(){
    popup.style.display = 'none'
}